@inherits BaseButtonComponent

<ButtonComponent ToolTipStr="@(ToolTipStr ?? "Cancel")"
                 Color="@(Color ?? "danger")"
                 Icon="@(Icon ?? "times")"
                 OnClick="OnClick"
                 Caption="@(Caption ?? "Cancel")"
                 IsOutline="@IsOutline"
                 IsSmall="@IsSmall" />