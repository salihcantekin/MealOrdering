@inherits BaseButtonComponent


<ButtonComponent Caption="@(Caption ?? "Add")"
                 Color="@(Color ?? "primary")"
                 Icon="@(Icon ?? "plus-square")"
                 OnClick="OnClick"
                 ToolTipStr="@(ToolTipStr ?? "Add")"
                 IsOutline="@IsOutline"
                 IsSmall="@IsSmall" />